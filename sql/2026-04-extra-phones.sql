-- 2026-04-20 — додаткові номери отримувача (UA + EU тощо).
--
-- У одного клієнта часто є 2-3 номери: український для UA-частини
-- маршруту і європейський для EU-частини. Зберігаємо їх як jsonb-масив
-- рядків у тій же таблиці `packages`, щоб не плодити окрему таблицю
-- phones. Первинний номер залишається у колонці `recipient_phone` —
-- це «головний» номер ліда, по якому робимо пошук та SMS.
--
-- extra_phones: ["+380631234567", "+48601234567"]

ALTER TABLE packages
    ADD COLUMN IF NOT EXISTS extra_phones jsonb DEFAULT '[]'::jsonb;
